# Optional: only used as a fallback during local development.
# The app's primary way to provide the Gemini API key is the Settings screen
# inside the app (stored in localStorage, never baked into the bundle).
# GEMINI_API_KEY="your-key-here"
